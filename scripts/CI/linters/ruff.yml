ruff:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4

    - name: Install Ruff
      run: pip install ruff

    - name: Run Ruff on Python files
      run: bash scripts/shell/linters/ruff.sh
