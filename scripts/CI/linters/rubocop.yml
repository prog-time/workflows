rubocop:
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4

    - name: Install RuboCop
      run: gem install rubocop

    - name: Run RuboCop on Ruby files
      run: bash scripts/shell/linters/rubocop.sh
